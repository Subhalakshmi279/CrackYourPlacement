Length of last word

class Solution {
    public int lengthOfLastWord(String s) {

        String arr[] = s.split(" ");

        int n = arr.length;

        String size = arr[n-1];

        return size.length();
        
    }
}
